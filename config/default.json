{
  "rateLimit": {
    "windowMs": 10000, // In milliseconds
    "max": 10, // limit each IP to 100 requests per `window`
    "standardHeaders": true, // Return rate limit info in the `RateLimit-*` headers
  }
}
